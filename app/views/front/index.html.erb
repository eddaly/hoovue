
<div class="container_12 index-header">
	<div class="grid_6 alpha">
		<h1>Welcome to hoovue.com</h1>
	
	</div>

		<div class="grid_6 push_1 search omega">
		<%= form_tag search_path, :method => 'get' do %>
  <%= text_field_tag :search, params[:search], data: {autocomplete_source: Product.order(:title).map(&:title)} %>
		 <%= submit_tag "Search", :name => nil, :class => "search-button" %>   


<% end %>
		</div>
</div>

<% if current_user %>

<%else%>
	<div class="white-box">
	
		<p>hoovue.com is a crowdsourced database and social network that allows Consumers and Creators to connect directly.</p>

		<p>To best make use of hoovue.com please  <%= link_to "register/login via Facebook.", "/auth/facebook", id: "login_with_facebook"  %> 
 	
	</div>
<%end%>

<% if current_user %>

<div class="container_12 credits-form">
	<% if @unclaimed.empty? %>
	<% else %>
	<h1>Unclaimed Credits</h1>
	<% end %>
	<div class= "activity-feed">
	 
		    	    	<ul>
	<% @unclaimed.each do |u| %>
	<li class="<%= cycle('even', 'odd') -%>"><h3><%= u.role %> <%= u.user_id %>on <%= u.product.title %></h3> <span class="right" style="margin-top:-80px;"><%= link_to "View Credit", front_validate_email_credit_url(:credit_id => u.id), :class => "blue-link" %> </span> </li>
	<%end%>	
</ul> 	


	</div>
</div>



<div class="container_12 credits-form">
	
	<% if @empty_credits.empty? %>
	<% else %>
	<h1>Your Credits</h1>
	<% end %>
<ul>
	<% @empty_credits.each do |c| %>
	<li class="<%= cycle('even', 'odd') -%>">
	<h2><% if c.product.image? %>
			<%= image_tag c.product.image.thumb.to_s %>
				 <%else%> 
			<%= image_tag '/assets/default-credit-small.jpg'%>
	 <%end%>  
	   
	 
	 
	  <%= c.role%><% if current_user %>			
<% if current_user.id == c.user_id %> <%= link_to "(edit)", role_credits_path(:id => c) %> <% end %> <% end %> on   <%= link_to c.product.title, product_path(c.product)%></h2>
	
		
		
		
	<% if c.credit_validations.empty? %>
		<% 3.times do %>
	<span class="score">
	<a href="#openModalemail"><%= image_tag '/assets/empty.png' %></a>
	</span>
		<% end %>
		<% else %>
	<% end %>
	
		
	<%=  %>	
	
	
				<% if c.credit_validations.count.eql?(1) %>
	<% c.credit_validations.each do |cv| %>
	<% if cv.status == "confirmed" %>
	<span class="score">
	<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/tick.png' %></a>
	</span>
	<% end %>
		<% if cv.status == "pending" %>
	<span class="score">
<%= image_tag '/assets/pending.png' %>
	
		</span>
		<% end %>
		<% 2.times do %>
	<span class="score">
	<a href="#openModalemail"><%= image_tag '/assets/empty.png' %></a>
	</span>
		<% end %>
		
		<% end %>
		<% else %>
	
<% end %>

<% if c.credit_validations.count.eql?(2)  %>
<% c.credit_validations.each do |cv| %>
<% if cv.status == "confirmed" %>
<span class="score">
<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/tick.png' %></a>
</span>
<% end %>
	<% if cv.status == "pending" %>
<span class="score">
<%= image_tag '/assets/pending.png' %>
	
	</span>
	<% end %>

		
	<% end %>
	<% 1.times do %>
<span class="score">
<a href="#openModalemail"><%= image_tag '/assets/empty.png' %></a>
</span>
	<% end %>
	
	<% end %>
	
	<% if c.credit_validations.count.eql?(3)  %>
	<% c.credit_validations.each do |cv| %>
	<% if cv.status == "confirmed" %>
	<span class="score">
	<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/tick.png' %></a>
	</span>
	<% end %>
		<% if cv.status == "pending" %>
	<span class="score">
	<%= image_tag '/assets/pending.png' %>
	
		</span>
		<% end %>

		
		<% end %>
	
	
		<% end %>

<br /><br /><br /><br />
	   
				
		<div class="more-detail">		
		<h3>Role description:</h3><p> <%= best_in_place c, :role_desc, :nil => "Click me to add content!", :type => :textarea, :ok_button => 'Save', :cancel_button => 'Cancel' %>
</p>	
			<h3>Interesting Fact: 
</h3><p><%= best_in_place c, :fact, :type => :textarea, :nil => "Click me to add content!", :ok_button => 'Save', :cancel_button => 'Cancel' %></p>
		</div>
	

	

<div class="container_12">

<%= link_to 'Delete', c, method: :delete, data: { confirm: 'Are you sure?' }, :class => "blue-link" %>
</div>
<div id="openModalemail" class="modalDialog">
	<div>
		<a href="#close" title="Close" class="close">X</a>
	<h2>E-mail Credit</h2>
<%= form_for @credit do |f|%>
	<%= f.label "Their E-mail"%>
	<%= f.text_field :pending_user_email %>
		<%= f.label "What was their role?"%>
	 	<%= f.text_field :role %>
	 		<%= f.hidden_field :product_id, :value => c.product.id %>
			<%= f.hidden_field :current_credit_id, :value => c.id %>
	 			<%= f.submit 'Create Your Credit', :id => 'blue-link' %>
				
	<% end %> 
	

</div>

<%end%>
	</li>
			
</ul>
</div>



<% if @credit_validations.empty? %>
<% else %>
<h1>Validation Requestions</h1>
<% end %>

<div class="container_12 credits-form">
	<ul>
	<% @credit_validations.each do |cv| %>
		<% c = Credit.find_by_id(cv.credit_id)%>
		<li class="<%= cycle('even', 'odd') -%>">
		<h2><% if c.product.image? %>
				<%= image_tag c.product.image.thumb.to_s %>
					 <%else%> 
				<%= image_tag '/assets/default-credit-small.jpg'%>
		 <%end%>  
	
  <%= c.user.name %> was a <%= c.role%>  on   <%= link_to c.product.title, product_path(c.product)%></h2>
   		
				
   		<% if cv.status == "confirmed" %>
   		<span class="score">
   		<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/tick.png' %></a>
   		</span>
   		<% end %>
   			<% if cv.status == "pending" %>
   		<span class="score">
   		<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/pending.png' %></a>
					
   				<% if current_user %>
   					<% if current_user.id == cv.validator_id %>
							
   					<%= link_to "Validate", validate_credit_validation_path(cv), :method=>:put  %>
   				<% end %>
   			<% end %>
   		<% end %>
   		</span>
   	
   <br /><br /><br /><br />
		<div class="more-detail">		
		<h3>Role description:</h3><p> <%= c.role_desc %></p>	
			<h3>Interesting Fact: 

</h3><p> <%= c.fact %></p>
		</div>  
		</li>
	<% end %>
	</ul>

</div>
<% if @flagged_credits.empty? %>
<% else %>
<h2>Your Pending Credits:</h2>
<% end %>
<div class="container_12 credits-form">
	<ul>
	<% @flagged_credits.each do |cv| %>
		<% c = Credit.find_by_id(cv.credit_id)%>
		<li class="<%= cycle('even', 'odd') -%>">
		<h2><% if c.product.image? %>
				<%= image_tag c.product.image.thumb.to_s %>
					 <%else%> 
				<%= image_tag '/assets/default-credit-small.jpg'%>
		 <%end%>  
	
  <%= c.user.name %> was a <%= c.role%>  on   <%= link_to c.product.title, product_path(c.product)%></h2>
   		
				
   		<% if cv.status == "confirmed" %>
   		<span class="score">
   		<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/tick.png' %></a>
   		</span>
   		<% end %>
   			<% if cv.status == "pending" %>
   		<span class="score">
   		<a href="/credit_validations/<%= cv.id %>"><%= image_tag '/assets/pending.png' %></a>
					
   				<% if current_user %>
   					<% if current_user.id == cv.validator_id %>
							
   					<%= link_to "Validate", validate_credit_validation_path(cv), :method=>:put  %>
   				<% end %>
   			<% end %>
   		<% end %>
   		</span>
   	
   <br /><br /><br /><br />
		<div class="more-detail">		
		<h3>Role description:</h3><p> <%= c.role_desc %></p>	
			<h3>Interesting Fact: 

</h3><p> <%= c.fact %></p>
		</div>  
		</li>
	<% end %>
	</ul>

</div>

<% end %>


<div class="product-list">
	<h2>Featured Users</h2>
		<ul class="product-list">
			<% @users_featured.each do |u| %>
		<%= link_to u do %>
<li class="product-list">
<span style="margin-left:40px;"><img src="http://graph.facebook.com/<%= u.uid%>/picture?type=small"></span>
<h3> <%= u.name %></h3>

</li>
<%end%><%end%>	
		</ul>
</div>

<div class="container_12 activity-feed">
	<h2>New Credits</h2>
		<ul>
			<% @credits.each do |c| %>
			<% if c.user %>
			<li class="<%= cycle('even', 'odd') -%>"><span class="timesince"><%= time_ago_in_words(c.created_at) %></span><img src="http://graph.facebook.com/<%= c.user.uid%>/picture?type=square" style="margin:5px;"> <%= link_to c.user.name, user_path(c.user) %> -   <%= link_to c.product.title, product_path(c.product)%>  <span class="right">Credit Added</span></li>
			<%else%>
			<%end%>
			<%end%>	
		</ul>
</div>
