<fieldset>
	<%= f.select :field_type, %w[text_field check_box] %>
	<%= f.text_field :name, placeholder: "field_name "%><br />
	 <%= f.label :required_field_? %> <%= f.check_box :required %>
	<%= f.hidden_field :_destroy %>
	<%= link_to "Remove", '#', class: "remove_fields"%>
</fieldset>
