<%= form_for(@product_genre) do |f| %>
  <% if @product_genre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_genre.errors.count, "error") %> prohibited this product_genre from being saved:</h2>

      <ul>
      <% @product_genre.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :genre_name %>
    <%= f.text_field :name %>
  </div>
  <%= f.fields_for :fields do |builder| %>
  	<%= render 'field_fields', f: builder%>
  <%end%>

  <%= link_to_add_fields "Add Field", f, :fields %>	<br />

  <div class="actions">
    <%= f.submit  %>
  </div>
<% end %>
