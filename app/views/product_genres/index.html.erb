<h1>Listing product_genres</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_genres.each do |product_genre| %>
  <tr>
    <td><%= product_genre.name %></td>
    <td><%= link_to 'Show', product_genre %></td>
    <td><%= link_to 'Edit', edit_product_genre_path(product_genre) %></td>
    <td><%= link_to 'Destroy', product_genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product genre', new_product_genre_path %>
